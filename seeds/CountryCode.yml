version: 2

seeds:
  - name: CountryCode
    config:
      column_types:
        Name: varchar(60)
        CountryCode: Number
        CountryCodeF:
          CountryCode2:
            column_types:
              Name: varchar(50)
              CountryCode: Number
